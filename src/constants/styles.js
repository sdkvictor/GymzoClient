import colors from './colors';

const globalStyles = {
    primaryButton: {
        backgroundColor: colors.primary,
		color: colors.white
    },
    secondaryButton: {
        backgroundColor: colors.primary,
		color: colors.white
    }
}

export default globalStyles